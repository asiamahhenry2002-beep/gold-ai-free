services:
  - type: web
    name: gold-ai-free-6
    env: python
    pythonVersion: 3.12.8  # Add this line
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    plan: free
